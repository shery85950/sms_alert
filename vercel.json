{
    "buildCommand": "cd frontend && echo 'No build needed'",
    "outputDirectory": "frontend",
    "rewrites": [
        {
            "source": "/",
            "destination": "/index.html"
        }
    ]
}
